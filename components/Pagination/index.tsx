import React from 'react'

type TPaginationProps = {
  currentPage: number
  maxPageLimit: number
  minPageLimit: number
}

const Pagination = ({
  currentPage,
  maxPageLimit,
  minPageLimit,
}: TPaginationProps) => {
  return <div></div>
}

export default Pagination
